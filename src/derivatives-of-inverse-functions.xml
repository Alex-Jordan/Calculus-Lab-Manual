<?xml version="1.0" encoding="UTF-8"?>

<!-- This file is part of the book -->
<!--                               -->
<!-- Calculus I Lab Manual         -->
<!--                               -->
<!-- Steve Simonds (author)        -->
<!-- Alex Jordan (editor)          -->

<section xml:id="section-derivatives-of-inverse-functions">
    <title>Derivatives of Inverse Functions</title>

    <p>Using <xref ref="definition-first-derivative"/>, it is easy to establish that <m>\lzoo{x}{x^2}=2x</m>. We can use this formula and implicit differentiation to find the formula for <m>\lzoo{x}{\sqrt{x}}</m>.  If <m>y=\sqrt{x}</m>, then <m>y^2=x</m> (and <m>y\geq0</m>). Using implicit differentiation we have:<md>
            <mrow>y^2&amp;=x</mrow>
            <mrow>\lzoo{x}{y^2}&amp;=\lzoo{x}{x}</mrow>
            <mrow>2y\lz{y}{x}&amp;=1</mrow>
            <mrow>\lz{y}{x}&amp;=\frac{1}{2y}\text{.}</mrow>
    </md></p>
    <p>But <m>y=\sqrt{x}</m>, so we have:<m>\begin{aligned}[t]\lz{y}{x}&amp;=\frac{1}{2y}\\&amp;=\frac{1}{2\sqrt{x}}\text{.}\end{aligned}</m></p>

    <p>In a similar manner, we can use the fact that <m>\lzoo{x}{\fe{\sin}{x}}=\fe{\cos}{x}</m> to come up with a formula for <m>\lzoo{x}{\fe{\sin^{-1}}{x}}</m>. If <m>y=\fe{\sin^{-1}}{x}</m>, then <m>\fe{\sin}{y}=x</m> and <m>-\frac{\pi}{2}\leq y\leq \frac{\pi}{2}</m>. Note that because of the angle range that <m>y</m> is confined to, that <m>\fe{\cos}{y}</m> is positive. So the Pythagorean identity <m>\fe{\cos^2}{y}=1-\fe{\sin^2}{y}</m> implies that <m>\fe{\cos}{y}=\sqrt{1-\fe{\sin^2}{y}}</m>. This gives us:<md>
    <mrow>\fe{\sin}{y}=x</mrow>
    <mrow>\lzoo{x}{\fe{\sin}{y}}&amp;=\lzoo{x}{x}</mrow>
    <mrow>\fe{\cos}{y}\lz{y}{x}&amp;=1</mrow>
    <mrow>\lz{y}{x}&amp;=\frac{1}{\fe{\cos}{y}}</mrow>
    <mrow>\lz{y}{x}&amp;=\frac{1}{\sqrt{1-\fe{\sin^2}{y}}}</mrow>
    <mrow>\lz{y}{x}&amp;=\frac{1}{\sqrt{1-x^2}}\text{.}</mrow></md></p>

    <exercises>
        <exercise>
            <statement>
                <p>Use the fact that <m>\lzoo{x}{e^x}=e^x</m> together with implicit differentiation to show that <m>\lzoo{x}{\fe{\ln}{x}}=\frac{1}{x}</m>. Begin by using the fact that <m>y=\fe{\ln}{x}</m> implies that <m>e^y=x</m> (and <m>x>0</m>).  Your first step is to differentiate both sides of the equation <m>e^y=x</m> with respect to <m>x</m>.</p>
            </statement>
        </exercise>
        <exercise>
            <statement>
                <p>Use the fact that <m>\lzoo{x}{\fe{\ln}{x}}=\frac{1}{x}</m> together with implicit differentiation to show that <m>\lzoo{x}{e^x}=e^x</m>. Begin by using the fact that <m>y=e^x</m> implies that <m>\fe{\ln}{y}=x</m>.  Your first step is to differentiate both sides of the equation <m>\fe{\ln}{y}=x</m> with respect to <m>x</m>.</p> 
            </statement>
        </exercise>
        <exercise>
            <statement>
                <p>Use the fact that <m>\lzoo{x}{\fe{\tan}{x}}=\fe{\sec^2}{x}</m> together with implicit differentiation to show that <m>\lzoo{x}{\fe{\tan^{-1}}{x}}=\frac{1}{1+x^2}</m>.  Begin by using the fact that <m>y=\fe{\tan^{-1}}{x}</m> implies that <m>\fe{\tan}{y}=x</m> (and <m>-\frac{\pi}{2}\lt y\lt\frac{\pi}{2}</m>).  Your first step is to differentiate both sides of the equation <m>\fe{\tan}{y}=x</m> with respect to <m>x</m>.  Please note that you will need to use the Pythagorean identity that relates the tangent and secant functions while working this problem.</p>
            </statement>
        </exercise>
    </exercises>
</section>
